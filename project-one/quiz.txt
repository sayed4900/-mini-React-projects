/*

Quiz!

1. What is a React component?
=> a function that returns React elements. (UI) and react elements is a javascrip object

2. What's wrong with this code?
```
function myComponent() {
    return (
        <small>I'm tiny text!</small>
    )
}
=> The name of the fuction does't start with capital letter
```

3. What's wrong with this code?
```
function Header() {
    return (
        <header>
            <nav>
                <img src="./react-logo.png" width="40px" />
            </nav>
        </header>
    )
}

ReactDOM.render(Header(), document.getElementById("root"))
=> funciton does't written on tag element ()
```

*/